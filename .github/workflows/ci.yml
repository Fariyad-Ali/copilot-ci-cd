name: Java CI

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Run tests
        run: mvn --batch-mode test

      - name: Build application
        run: mvn --batch-mode package -DskipTests

  docker-publish:
    needs: test-and-build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/copilot-ci-cd

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and publish Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:1.0
            ${{ env.IMAGE_NAME }}:1.0.0
            ${{ env.IMAGE_NAME }}:1.0.0-SNAPSHOT
            ${{ env.IMAGE_NAME }}:1.0.0-RELEASE
            ${{ env.IMAGE_NAME }}:1.0.0-RC1
            ${{ env.IMAGE_NAME }}:1.0.0-RC2
            ${{ env.IMAGE_NAME }}:1.0.0-RC3
            ${{ env.IMAGE_NAME }}:1.0.0-RC4
            ${{ env.IMAGE_NAME }}:1.0.0-RC5
            ${{ env.IMAGE_NAME }}:1.0.0-RC6
            ${{ env.IMAGE_NAME }}:1.0.0-RC7
            ${{ env.IMAGE_NAME }}:1.0.0-RC8
            ${{ env.IMAGE_NAME }}:1.0.0-RC9
            ${{ env.IMAGE_NAME }}:1.0.0-RC10